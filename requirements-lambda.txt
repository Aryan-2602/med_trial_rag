# Lambda runtime dependencies (no indexing tools)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
boto3>=1.28.0
faiss-cpu>=1.7.4
numpy>=1.24.0
httpx>=0.25.0
tenacity>=8.2.0
python-dotenv>=1.0.0
structlog>=23.2.0
mangum>=0.17.0

# Note: pandas, pypdf, pyreadstat are NOT needed for Lambda runtime
# They're only used when building indices locally
