# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
httpx>=0.25.0
tenacity>=8.2.0
python-dotenv>=1.0.0
structlog>=23.2.0
mysql-connector-python>=8.2.0

# Vector database
chromadb>=0.4.22

# OpenAI for embeddings
openai>=1.12.0

# Note: pandas, pypdf, pyreadstat are only needed when building indices
# They're installed separately when needed